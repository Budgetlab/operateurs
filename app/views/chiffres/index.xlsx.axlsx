
    wb = xlsx_package.workbook
    s = wb.styles
    header = s.add_style bg_color: 'c3cffa'
  	wb.add_worksheet(name: "Chiffres clés") do |sheet| 	
    	sheet.add_row %W{ Exercice Budget Date\ de\ saisie Phase Examen}, style: header

      @chiffres.each do |chiffre|
      examen = chiffre.statut == 'valide' ? chiffre.risque_insolvabilite : 'Brouillon'
        sheet.add_row [chiffre.exercice_budgetaire, chiffre.type_budget, chiffre.created_at, chiffre.phase, examen ]
      end
  	end
 